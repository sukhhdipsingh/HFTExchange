// engine.cpp
// entry point for the hft_engine static library.
// right now this file is mostly a placeholder — the actual implementations
// live in the headers (.h + .inl) because MemoryPool is a template.
// non-template modules like OrderBook will add their .cpp here as we go.

// this translation unit is intentionally small for now.
// do not remove it — CMake needs at least one source file in the library
// target.
